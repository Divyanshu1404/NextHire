# Frontend Coding Standards

- Use the shared HTTP client for all API calls.
- Keep API logic out of components.
- Store request-wide state in Redux slices when it is reused.
- Use hooks for cross-cutting concerns like auth hydration.
- Preserve compatibility wrappers while migrating older imports.
