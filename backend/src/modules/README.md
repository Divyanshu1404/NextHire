Modules: Feature-based grouping for controllers/services/repositories when useful.

Example: `modules/company` may contain `company.controller.js`, `company.service.js`, `company.repository.js`, and `company.validation.js`.
