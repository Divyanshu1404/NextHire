Repositories: Encapsulate all database access using Mongoose models.

Responsibilities:
- CRUD operations
- Query builders and pagination helpers
- Avoid business rules here — return raw data to services
