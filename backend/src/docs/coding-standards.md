# Coding Standards

- Keep controllers thin.
- Put business rules in services.
- Put database access in repositories.
- Use `AppError` for operational failures.
- Use `sendSuccess` for JSON responses.
- Prefer shared helpers over repeated logic.
- Add schema indexes for dashboard and search queries.
