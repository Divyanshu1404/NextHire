Controllers: Lightweight request handlers that call services and return responses.

Place controller files here, e.g. `auth.controller.js`, `job.controller.js`.
Controllers should not contain business logic — delegate to services.
